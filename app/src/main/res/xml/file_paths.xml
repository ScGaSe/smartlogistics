<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 应用私有目录下的 cache 文件夹 -->
    <cache-path
        name="cache"
        path="." />

    <!-- 应用私有目录下的 files 文件夹 -->
    <files-path
        name="files"
        path="." />

    <!-- 外部存储的图片目录 -->
    <external-path
        name="external"
        path="." />

    <!-- 外部存储的 Pictures 目录 -->
    <external-files-path
        name="external_files"
        path="." />

    <!-- 专门用于相机拍照的目录 -->
    <external-cache-path
        name="external_cache"
        path="." />
</paths>
